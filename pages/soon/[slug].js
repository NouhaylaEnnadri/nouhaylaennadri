// pages/soon/[slug].js

import { ComingSoon } from "@/components";

/**
 * `SoonPage` Component
 *
 * This component renders a "Coming Soon" page. It uses the `ComingSoon` component
 * to display a placeholder for upcoming content that is not yet available.
 *
 * @returns {JSX.Element} The rendered `ComingSoon` component.
 */
const SoonPage = () => {
  return <ComingSoon />;
};

export default SoonPage;
